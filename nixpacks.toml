[phases.setup]
nixPkgs = ["nodejs_20", "nss", "nspr", "cups", "libdrm", "libxkbcommon", "pango", "cairo", "gdk-pixbuf", "mesa", "alsa-lib"]

[phases.install]
cmds = ["npm ci", "npx playwright install chromium --with-deps"]
